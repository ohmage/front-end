<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
          {{name}}
          {{#if this.ownedByCurrentUser}}
            {{#link-to 'survey.edit' this }}<button class="btn btn-primary pull-right">Edit</button>{{/link-to}}
          {{else}}
            {{#link-to 'survey.clone' this }}<button class="btn btn-primary pull-right">Clone</button>{{/link-to}}
          {{/if}}
        </h1>
        <p>{{description}}</p>
    </div>
    <!-- /.col-lg-12 -->
</div>

<div class="panel panel-default">
  <div class="panel-heading">
      <i class="fa fa-bar-chart-o fa-fw"></i> Survey Items
  </div>
  <!-- /.panel-heading -->
  <div class="panel-body">
    <pre>{{survey_items}}</pre>
  </div>
  <!-- /.panel-body -->
</div>